import { TIMING_CATEGORIES } from '../../core/constants/operationTemplates';
import { reportStatus } from '../../core/events/uiEvents';
import type { ConnectionType, ProcessConnection } from '../../models/connections/ProcessConnection';
import type { OperationInstance, OperationInstancePatch } from '../../models/operations/OperationInstance';
import type { OperationType } from '../../models/operations/OperationTemplate';
import type { PlacedResource, PlacedResourcePatch } from '../../models/resources/PlacedResource';
import type { SelectionController } from '../../models/selection/Selection';
import type { ConnectionStore } from '../../services/ConnectionStore';
import { validateProcessConnections } from '../../services/ConnectionValidation';
import { MIN_OPERATION_HEIGHT, MIN_OPERATION_WIDTH, type OperationStore } from '../../services/OperationStore';
import { validateOperations } from '../../services/OperationValidation';
import { routeBendCount, routeLength } from '../../services/OrthogonalRouter';
import { MIN_RESOURCE_HEIGHT, MIN_RESOURCE_WIDTH, type ResourceStore } from '../../services/ResourceStore';
import { actionButton, element } from '../../ui/dom';

export interface RightSidebarController { readonly element: HTMLElement; dispose(): void; }
function section(title: string, content: HTMLElement, open = true): HTMLDetailsElement { const details = element('details', 'inspector-section'); details.open = open; details.append(element('summary', '', title), content); return details; }
function field(labelText: string, control: HTMLElement): HTMLLabelElement { const label = element('label', 'property-field'); label.append(element('span', '', labelText), control); return label; }
function input(type: 'text' | 'number', minimum?: number): HTMLInputElement { const control = element('input', 'property-input'); control.type = type; if (type === 'number') control.step = '1'; if (minimum !== undefined) control.min = String(minimum); return control; }
function checkbox(label: string): HTMLInputElement { const control = element('input', 'property-checkbox'); control.type = 'checkbox'; control.setAttribute('aria-label', label); return control; }
function summaryRow(container: HTMLElement, label: string, value: string): void { container.append(element('span', '', label), element('strong', '', value)); }
function option(value: string): HTMLOptionElement { const node = element('option', '', value); node.value = value; return node; }

export function createRightSidebar(resourceStore: ResourceStore, operationStore: OperationStore, connectionStore: ConnectionStore, selectionStore: SelectionController): RightSidebarController {
  const sidebar = element('aside', 'sidebar sidebar--right'); sidebar.setAttribute('aria-label', 'Inspector panels'); const properties = element('div', 'properties-content'); const summary = element('div', 'summary-grid'); const validation = element('div', 'validation-summary'); sidebar.append(section('Properties', properties), section('Selection Summary', summary), section('Validation Summary', validation));
  const updateResource = (patch: PlacedResourcePatch, label: string): void => { const resource = resourceStore.getSelectedResource(); if (resource) reportStatus(resourceStore.updateResource(resource.id, patch) ? `${label} updated` : `Invalid ${label.toLocaleLowerCase()} value`); };
  const updateOperation = (patch: OperationInstancePatch, label: string): void => { const operation = operationStore.getSelectedOperation(); if (operation) reportStatus(operationStore.updateOperation(operation.id, patch) ? `${label} updated` : `Invalid ${label.toLocaleLowerCase()} value`); };
  const bindNumber = (control: HTMLInputElement, commit: (value: number) => boolean, minimum: number, integer = false): void => { const run = (): void => { const value = Number(control.value); const valid = Number.isFinite(value) && value >= minimum && (!integer || Number.isInteger(value)); control.setAttribute('aria-invalid', String(!valid)); if (valid && !commit(value)) control.setAttribute('aria-invalid', 'true'); }; control.addEventListener('change', run); control.addEventListener('blur', run); control.addEventListener('keydown', (event) => { if (event.key === 'Enter') { event.preventDefault(); run(); } }); };
  const toggle = (labelText: string, control: HTMLInputElement): HTMLLabelElement => { const label = element('label', 'property-toggle'); label.append(control, element('span', '', labelText)); return label; };
  const bindName = (control: HTMLInputElement, commit: () => void): void => { control.addEventListener('change', commit); control.addEventListener('blur', commit); control.addEventListener('keydown', (event) => { if (event.key === 'Enter') { event.preventDefault(); commit(); } }); };

  const renderResource = (resource: PlacedResource): void => {
    const form = element('form', 'properties-form'); form.addEventListener('submit', (event) => event.preventDefault()); const name = input('text'); name.value = resource.name; bindName(name, () => updateResource({ name: name.value.trim() }, 'Resource name')); const type = element('output', 'property-output', resource.resourceType);
    const x = input('number'); x.value = String(resource.worldX); bindNumber(x, (value) => resourceStore.updateResource(resource.id, { worldX: value }), Number.NEGATIVE_INFINITY); const y = input('number'); y.value = String(resource.worldY); bindNumber(y, (value) => resourceStore.updateResource(resource.id, { worldY: value }), Number.NEGATIVE_INFINITY); const width = input('number', MIN_RESOURCE_WIDTH); width.value = String(resource.width); bindNumber(width, (value) => resourceStore.updateResource(resource.id, { width: value }), MIN_RESOURCE_WIDTH); const height = input('number', MIN_RESOURCE_HEIGHT); height.value = String(resource.height); bindNumber(height, (value) => resourceStore.updateResource(resource.id, { height: value }), MIN_RESOURCE_HEIGHT); x.disabled = resource.locked; y.disabled = resource.locked;
    const locked = checkbox('Locked'); locked.checked = resource.locked; locked.addEventListener('change', () => updateResource({ locked: locked.checked }, 'Lock state')); const visible = checkbox('Visible'); visible.checked = resource.visible; visible.addEventListener('change', () => updateResource({ visible: visible.checked }, 'Visibility')); const toggles = element('div', 'property-toggles'); toggles.append(toggle('Locked', locked), toggle('Visible', visible)); form.append(field('Resource name', name), field('Resource type', type), field('X position', x), field('Y position', y), field('Width', width), field('Height', height), toggles); properties.replaceChildren(form);
  };
  const renderOperation = (operation: OperationInstance): void => {
    const form = element('form', 'properties-form operation-properties'); form.addEventListener('submit', (event) => event.preventDefault()); const name = input('text'); name.value = operation.name; bindName(name, () => updateOperation({ name: name.value.trim() }, 'Operation name'));
    const type = element('select', 'property-input'); [...new Set(operationStore.getTemplates().map((template) => template.operationType))].forEach((value) => type.append(option(value))); type.value = operation.operationType; type.addEventListener('change', () => updateOperation({ operationType: type.value as OperationType }, 'Operation type')); const timing = element('select', 'property-input'); TIMING_CATEGORIES.forEach((value) => timing.append(option(value))); timing.value = operation.timingCategory; timing.addEventListener('change', () => updateOperation({ timingCategory: timing.value as OperationInstance['timingCategory'] }, 'Timing category'));
    const cycle = input('number', 0.001); cycle.step = '0.001'; cycle.value = String(operation.cycleTimeSeconds); bindNumber(cycle, (value) => operationStore.updateOperation(operation.id, { cycleTimeSeconds: value }), 0.001); const sequence = input('number', 1); sequence.value = String(operation.sequence); bindNumber(sequence, (value) => operationStore.updateOperation(operation.id, { sequence: value }), 1, true);
    const assignment = element('select', 'property-input'); const unassigned = option('Unassigned'); unassigned.value = ''; assignment.append(unassigned); resourceStore.getPlacedResources().forEach((resource) => { const item = option(`${resource.name} (${resource.id})`); item.value = resource.id; assignment.append(item); }); assignment.value = operation.assignedResourceId ?? ''; assignment.addEventListener('change', () => updateOperation({ assignedResourceId: assignment.value || null }, 'Resource assignment')); const notes = element('textarea', 'property-input property-textarea'); notes.value = operation.notes; notes.rows = 3; notes.addEventListener('change', () => updateOperation({ notes: notes.value }, 'Notes'));
    const x = input('number'); x.value = String(operation.worldX); bindNumber(x, (value) => operationStore.updateOperation(operation.id, { worldX: value }), Number.NEGATIVE_INFINITY); const y = input('number'); y.value = String(operation.worldY); bindNumber(y, (value) => operationStore.updateOperation(operation.id, { worldY: value }), Number.NEGATIVE_INFINITY); const width = input('number', MIN_OPERATION_WIDTH); width.value = String(operation.width); bindNumber(width, (value) => operationStore.updateOperation(operation.id, { width: value }), MIN_OPERATION_WIDTH); const height = input('number', MIN_OPERATION_HEIGHT); height.value = String(operation.height); bindNumber(height, (value) => operationStore.updateOperation(operation.id, { height: value }), MIN_OPERATION_HEIGHT); x.disabled = operation.locked; y.disabled = operation.locked;
    const locked = checkbox('Locked'); locked.checked = operation.locked; locked.addEventListener('change', () => updateOperation({ locked: locked.checked }, 'Lock state')); const visible = checkbox('Visible'); visible.checked = operation.visible; visible.addEventListener('change', () => updateOperation({ visible: visible.checked }, 'Visibility')); const toggles = element('div', 'property-toggles'); toggles.append(toggle('Locked', locked), toggle('Visible', visible)); form.append(field('Operation name', name), field('Operation type', type), field('Timing category', timing), field('Cycle time (seconds)', cycle), field('Sequence', sequence), field('Assigned resource', assignment), field('Notes', notes), field('X position', x), field('Y position', y), field('Width', width), field('Height', height), toggles); properties.replaceChildren(form);
  };
  const renderConnection = (connection: ProcessConnection): void => {
    const form = element('form', 'properties-form connection-properties'); form.addEventListener('submit', (event) => event.preventDefault()); const sourceOperation = operationStore.getOperation(connection.sourceOperationId); const targetOperation = operationStore.getOperation(connection.targetOperationId);
    const id = element('output', 'property-output', connection.id); const source = element('output', 'property-output', sourceOperation ? `OP ${sourceOperation.sequence} — ${sourceOperation.name}` : connection.sourceOperationId); const target = element('output', 'property-output', targetOperation ? `OP ${targetOperation.sequence} — ${targetOperation.name}` : connection.targetOperationId);
    const type = element('select', 'property-input'); (['Standard', 'Rework', 'Alternate', 'Information'] as const).forEach((value) => type.append(option(value))); type.value = connection.connectionType; type.addEventListener('change', () => reportStatus(connectionStore.updateConnection(connection.id, { connectionType: type.value as ConnectionType }) ? 'Connection type updated' : 'Duplicate Standard connection not permitted'));
    const label = input('text'); label.maxLength = 120; label.value = connection.label; bindName(label, () => reportStatus(connectionStore.updateConnection(connection.id, { label: label.value }) ? 'Connection label updated' : 'Invalid connection label'));
    const sourceAnchor = element('output', 'property-output', `${connection.sourceAnchor.side} @ ${connection.sourceAnchor.offset.toFixed(3)}`); const targetAnchor = element('output', 'property-output', `${connection.targetAnchor.side} @ ${connection.targetAnchor.offset.toFixed(3)}`); const routeStatus = element('output', 'property-output', connection.routeStatus === 'clear' ? 'Clear route' : 'Fallback routing warning'); const length = element('output', 'property-output', `${routeLength(connection.routePoints).toFixed(1)} units`); const bends = element('output', 'property-output', String(routeBendCount(connection.routePoints)));
    const locked = checkbox('Locked'); locked.checked = connection.locked; locked.addEventListener('change', () => { connectionStore.updateConnection(connection.id, { locked: locked.checked }); reportStatus(`Connection ${locked.checked ? 'locked' : 'unlocked'}`); }); const visible = checkbox('Visible'); visible.checked = connection.visible; visible.addEventListener('change', () => { connectionStore.updateConnection(connection.id, { visible: visible.checked }); reportStatus(`Connection ${visible.checked ? 'shown' : 'hidden'}`); }); const toggles = element('div', 'property-toggles'); toggles.append(toggle('Locked', locked), toggle('Visible', visible));
    const actions = element('div', 'connection-property-actions'); const reverse = actionButton('Reverse Direction', 'secondary-button'); const remove = actionButton('Delete Connection', 'secondary-button connection-delete-button'); reverse.addEventListener('click', () => { const result = connectionStore.reverseConnection(connection.id); reportStatus(result === 'updated' ? 'Connection reversed' : result === 'duplicate' ? 'Cannot reverse: duplicate Standard connection' : result === 'locked' ? 'Connection is locked' : 'Connection not found'); }); remove.addEventListener('click', () => { const result = connectionStore.deleteConnection(connection.id); reportStatus(result === 'deleted' ? 'Connection deleted' : result === 'locked' ? 'Connection is locked' : 'Connection not found'); }); actions.append(reverse, remove);
    form.append(field('Connection ID', id), field('Source operation', source), field('Target operation', target), field('Connection type', type), field('Label', label), field('Source anchor', sourceAnchor), field('Target anchor', targetAnchor), field('Route status', routeStatus), field('Route length', length), field('Bend count', bends), toggles, actions); properties.replaceChildren(form);
  };
  const render = (): void => {
    const resource = resourceStore.getSelectedResource(); const operation = operationStore.getSelectedOperation(); const connection = connectionStore.getSelectedConnection();
    if (resource) renderResource(resource); else if (operation) renderOperation(operation); else if (connection) renderConnection(connection); else { const empty = element('div', 'empty-inspector'); empty.append(element('strong', '', 'No selection'), element('p', '', 'Select a resource, operation, or process connection to edit its properties.')); properties.replaceChildren(empty); }
    summary.replaceChildren(); if (resource) { summaryRow(summary, 'Object type', 'Resource'); summaryRow(summary, 'ID', resource.id); summaryRow(summary, 'Name', resource.name); summaryRow(summary, 'Position', `${resource.worldX.toFixed(1)}, ${resource.worldY.toFixed(1)}`); }
    else if (operation) { summaryRow(summary, 'Object type', 'Operation'); summaryRow(summary, 'ID', operation.id); summaryRow(summary, 'Sequence', `OP ${operation.sequence}`); summaryRow(summary, 'Cycle time', `${operation.cycleTimeSeconds}s`); summaryRow(summary, 'Resource', operation.assignedResourceId ? resourceStore.getResource(operation.assignedResourceId)?.name ?? 'Missing' : 'Unassigned'); }
    else if (connection) { const sourceOperation = operationStore.getOperation(connection.sourceOperationId); const targetOperation = operationStore.getOperation(connection.targetOperationId); summaryRow(summary, 'Object type', 'Connection'); summaryRow(summary, 'ID', connection.id); summaryRow(summary, 'Type', connection.connectionType); summaryRow(summary, 'Source', `${sourceOperation?.id ?? connection.sourceOperationId} — ${sourceOperation?.name ?? 'Missing'}`); summaryRow(summary, 'Target', `${targetOperation?.id ?? connection.targetOperationId} — ${targetOperation?.name ?? 'Missing'}`); summaryRow(summary, 'Label', connection.label || '—'); summaryRow(summary, 'Route length', routeLength(connection.routePoints).toFixed(1)); summaryRow(summary, 'Bends', String(routeBendCount(connection.routePoints))); summaryRow(summary, 'Locked', connection.locked ? 'Yes' : 'No'); summaryRow(summary, 'Visible', connection.visible ? 'Yes' : 'No'); summaryRow(summary, 'Validation', connection.routeStatus === 'clear' ? 'Clear' : 'Warning'); }
    else { summaryRow(summary, 'Selected items', '0'); summaryRow(summary, 'Object type', '—'); }
    const operationResult = validateOperations(operationStore.getOperations(), (id) => Boolean(resourceStore.getResource(id))); const processResult = validateProcessConnections(operationStore.getOperations(), connectionStore.getConnections()); const errors = operationResult.errors + processResult.errors; const warnings = operationResult.warnings + processResult.warnings; validation.replaceChildren(element('div', !errors && !warnings ? 'validation-healthy' : errors ? 'validation-error' : 'validation-warning', !errors && !warnings ? '● Project healthy' : `● ${errors ? 'Project has errors' : 'Review warnings'}`)); const metrics = element('div', 'validation-metrics'); metrics.append(element('span', '', `${errors} Errors`), element('span', '', `${warnings} Warnings`)); validation.append(metrics);
    const issues = operation ? operationResult.issues.filter((issue) => issue.operationId === operation.id) : connection ? processResult.issues.filter((issue) => issue.connectionId === connection.id) : []; if (issues.length) { const list = element('ul', 'validation-issues'); issues.forEach((issue) => list.append(element('li', `validation-issue validation-issue--${issue.severity}`, issue.message))); validation.append(list); }
  };
  const unsubscribers = [resourceStore.subscribe(render), operationStore.subscribe(render), connectionStore.subscribe(render), selectionStore.subscribe(render)]; render(); return { element: sidebar, dispose: () => unsubscribers.forEach((unsubscribe) => unsubscribe()) };
}
